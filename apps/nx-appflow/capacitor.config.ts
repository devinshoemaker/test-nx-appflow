import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'text-nx-appflow',
  webDir: '../../dist/apps/nx-appflow',
  bundledWebRuntime: false
};

export default config;
